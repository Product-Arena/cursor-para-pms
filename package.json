{
  "name": "cursor-para-pms",
  "private": true,
  "description": "Product Arena — Cursor na prática (slides, guia do aluno, case Arena Cash)",
  "scripts": {
    "build:guia": "rm -rf dist && cp -R guia-do-aluno dist && rm -f dist/.replit dist/REPLIT.md dist/serve-local.sh dist/start-my-day-arenacash-reference.md"
  }
}
