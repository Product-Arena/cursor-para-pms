# Publishes student guide (guia-do-aluno/) to GitHub Pages.
# Instructor slides (docs/) are local-only and not in this repository.
name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Assemble site
        run: |
          mkdir -p _site/guia-do-aluno
          rsync -a guia-do-aluno/ _site/guia-do-aluno/ \
            --exclude '.github' \
            --exclude '.replit' \
            --exclude 'REPLIT.md' \
            --exclude '.DS_Store'
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
